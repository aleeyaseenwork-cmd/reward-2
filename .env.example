# Discord bot token — Developer Portal → your application → Bot → Reset Token
BOT_TOKEN=your-bot-token

# Application (Client) ID — Developer Portal → your application → General Information
CLIENT_ID=your-application-client-id

# MongoDB connection string (Atlas free tier works fine)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/rewardbot
